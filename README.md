# AquaFlow 水瓶排序（Web 版）

一个基于网页的水排序益智游戏，点击「开始游戏」即可进入随机关卡挑战。没有关卡列表，没有难度选择——难度由系统根据你的表现动态调节，通关后才会揭晓刚才那一局是什么难度。

---

## ✨ 特性

### 🎮 核心玩法
- **隐藏难度**：每次开局随机生成关卡，玩家不知道当前难度
- **自适应难度系统（DDA）**：连胜自动升档，频繁跳过/提示则自动降档
- **正向随机生成 + BFS 验证**：确保每局可解，杜绝死局
- **无限撤销**：随时回退到上一步
- **智能提示**：基于 BFS 最短解给出最优下一步
- **跳关功能**：卡住了可以直接开新的一局

### 🎨 视觉与交互
- **点击操作**：选中瓶子 → 再点目标瓶子完成倒水
- **动画反馈**：
  - 选中时瓶子上浮发光
  - 非法操作触发摇晃提示
  - 通关时彩带飘落
- **日夜主题**：一键切换，偏好自动记忆
- **修长玻璃瓶**：高对比颜色 + 段间分隔线，避免看花

### 📊 数据持久化
- 连胜纪录、总场次、胜率
- 主题偏好
- 全部存储在 `localStorage`，无需注册

---

## 🚀 运行方式

无需构建，直接打开：

```bash
# 方式一：直接双击 index.html 用浏览器打开

# 方式二：本地启动（推荐，避免模块加载限制）
python -m http.server 8080
# 然后访问 http://localhost:8080
```

> 本项目仅针对桌面浏览器优化，推荐在电脑端游玩。

---

## 🛠️ 技术栈

- **框架**：原生 JavaScript（ES6 Modules）
- **样式**：CSS Variables + Flex/Grid
- **状态存储**：LocalStorage
- **构建**：无，零依赖，开箱即用

---

## 📁 目录结构

```
├── index.html              # 入口
├── DESIGN.md               # 设计文档（难度模型、架构说明）
├── readme.md               # 本文件
├── styles/
│   ├── main.css            # 基础样式与动画
│   ├── themes.css          # 主题变量（日间/夜间）
│   └── responsive.css      # 响应式规则
└── scripts/
    ├── core.js             # 游戏主循环、状态管理、自适应难度
    ├── generator.js        # 关卡生成器（正向随机 + BFS 验证）
    ├── solver.js           # BFS 最短解求解器 + 难度评估
    ├── ui.js               # 渲染、动画、事件绑定
    └── layout.js           # 响应式布局计算
```

---

## 🧩 难度模型（内部）

系统维护 5 档隐藏难度，由三维度评分：

| 档位 | 颜色数 | 空瓶数 | BFS 最短解步数 |
|-----|-------|-------|--------------|
| 入门 | 3–4 | 2–3 | 3–15 |
| 简单 | 4–5 | 2–3 | 8–25 |
| 中等 | 5–7 | 1–2 | 15–40 |
| 困难 | 6–8 | 1–2 | 20–60 |
| 极难 | 7–9 | 1 | 30–90 |

> 玩家连胜、未使用提示 → 升档；频繁跳过/提示 → 降档。

---

## 📝 版本

**v1.0.0** — 基础玩法完整版  
已实现：点击交互、随机关卡生成、BFS 求解与难度评估、自适应难度、撤销/提示/跳关、主题切换、通关特效、响应式布局。
