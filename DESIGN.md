# AquaFlow 水排序网页版 — 设计文档

## 1. 核心设计理念

### 1.1 摒弃传统关卡制
- **无关卡列表**，无 "第1关、第2关"
- **无难度选择按钮**，玩家只需要点击「开始游戏」
- 难度由系统后台随机生成并动态调节，玩家通关后才得知"刚才那局是什么难度"

### 1.2 自适应难度系统 (DDA)
系统内部维护一个隐藏的难度值，根据玩家表现动态调整：

| 玩家状态 | 系统行为 |
|---------|---------|
| 连胜 3+ 局，且平均用时短 | 逐步提高生成参数（更多颜色、更少空瓶、更深混乱度） |
| 有输有赢，用时正常 | 维持当前难度区间 |
| 连续跳过/使用提示/超时 | 降低难度，增加空瓶或减少颜色 |

**难度不显示为"简单/困难"**，胜利后可俏皮地告诉玩家："这局其实挺棘手的！"

---

## 2. 难度界定模型

### 2.1 难度由什么决定？

水排序的真正难度不是"颜色数量"这一个维度，而是**解题路径的复杂程度**。我们采用三维度评分：

1. **颜色数 (C)**：4~10 种，越多越难
2. **空瓶数 (E)**：1~3 个，越少越难（中转空间小）
3. **最少解题步数 (S)**：通过 BFS 求解器计算的最短步数

### 2.2 难度评分公式

```
DifficultyScore = S × (1 + (C - 4) × 0.1) × (1 + (3 - E) × 0.15)
```

| 分数区间 | 隐藏标签 | 玩家感知 |
|---------|---------|---------|
| < 15 | 入门 | 轻松愉快 |
| 15~30 | 简单 | 小有挑战 |
| 30~50 | 中等 | 需要思考 |
| 50~80 | 困难 | 相当烧脑 |
| > 80 | 极难 | 可能卡很久 |

### 2.3 关卡生成策略（反向生成法）

为了保证**100%可解**且避免复杂的死局验证，我们采用**反向生成**：

1. 从"已解决状态"出发：每种颜色一个满瓶，外加空瓶
2. 执行 N 次**随机合法倒水**（把已排序的瓶子打乱）
3. 得到的状态一定可解（反向操作就是解法）
4. 用 BFS 计算最短解步数 S
5. 如果 S 落在目标难度区间内，接受该关卡；否则重新生成

**为什么反向生成优于随机打乱？**
- 随机打乱可能生成死局（无解关卡）
- 反向生成保证有解，且通过调整 N 可以控制复杂度下限
- BFS 验证确保难度评分准确

---

## 3. 界面设计方案

### 3.1 开始界面
```
┌─────────────────────────┐
│                         │
│    [缓缓流动的液体动画]   │
│                         │
│       AquaFlow          │
│      水瓶排序 puzzle     │
│                         │
│    ┌─────────────┐      │
│    │  开始游戏   │  ← 唯一按钮
│    └─────────────┘      │
│                         │
│    已连胜 5 局 🔥       │
│                         │
└─────────────────────────┘
```
- 极简，只有一个按钮
- 底部显示历史连胜纪录，营造挑战感

### 3.2 游戏界面
```
┌─────────────────────────┐
│  ↺  💡  🚀    时间 1:23 │
│              步数 15    │
├─────────────────────────┤
│                         │
│     🍶  🍶  🍶  🍶      │
│                         │
│     🍶  🍶  🍶  🍶      │
│                         │
├─────────────────────────┤
│ [撤销] [提示] [新一局]  │
└─────────────────────────┘
```

**交互细节**：
- **点击模式**：点击选中瓶子（上浮+发光），再点击目标瓶子倒水
- **拖拽模式**：拖动到目标瓶子上方松手（移动端更自然）
- **选中反馈**：选中瓶子 scale(1.08) + 金色边框光晕
- **错误反馈**：尝试非法倒水时，瓶子左右摇晃（shake动画）
- **倒水动画**：液体高度变化使用 CSS transition，视觉上看得到"流入"
- **空瓶质感**：半透明玻璃感，能看到背景色

### 3.3 胜利界面
```
┌─────────────────────────┐
│      🎉 太棒了！ 🎉      │
│                         │
│     用时 2 分 15 秒      │
│     步数 23             │
│                         │
│   这局的难度：中等 🧠     │
│                         │
│    ┌─────────────┐      │
│    │  再来一局   │      │
│    └─────────────┘      │
└─────────────────────────┘
```
- 显示烟花/彩带动画（CSS particles）
- 揭晓本局隐藏难度，给玩家"原来如此"的恍然大悟感

---

## 4. 技术架构

### 4.1 模块职责

| 文件 | 职责 |
|-----|------|
| `core.js` | 游戏主循环、状态管理、自适应难度逻辑、本地存储 |
| `generator.js` | 反向生成关卡、难度参数调控、颜色池管理 |
| `solver.js` | BFS最短解求解器、难度评分计算、死局检测 |
| `ui.js` | DOM渲染、动画效果（倒水/选中/错误/胜利）、事件绑定 |
| `layout.js` | 响应式布局计算、瓶子网格排列、视口适配 |
| `themes.css` | 主题变量（日间/夜间） |
| `main.css` | 基础样式、动画关键帧 |
| `responsive.css` | 媒体查询、移动端适配 |

### 4.2 状态流转

```
开始界面 → [点击开始] → 生成关卡 → BFS验证难度 → 游戏界面
                                      ↓
胜利界面 ← [检测胜利] ← 玩家操作 ← 渲染瓶子
   ↓
[点击再来一局] → 记录战绩 → 调整难度参数 → 生成新关卡
```

---

## 5. 关键算法细节

### 5.1 BFS 求解器
- **状态表示**：每个瓶子颜色数组 → JSON字符串序列化
- **状态去重**：瓶子位置无关性优化（对瓶子表示排序后统一）
- **剪枝策略**：
  1. 禁止立即回退（不把刚倒出的水倒回去）
  2. 禁止无意义移动（将满瓶的同色液体倒入另一个未满的同色瓶，如果能一次倒满才允许）
  3. 禁止向空瓶倒单段液体（除非该段能继续被合并）
- **搜索上限**：最多搜索 50000 个状态，超时则认为极难

### 5.2 自适应难度参数表

```javascript
const DIFFICULTY_BRACKETS = [
    { label: '入门', minSteps: 5,  maxSteps: 15, colors: [3,4],   empty: [2,3], shuffle: [5, 15] },
    { label: '简单', minSteps: 15, maxSteps: 30, colors: [4,5],   empty: [2,3], shuffle: [15, 30] },
    { label: '中等', minSteps: 30, maxSteps: 50, colors: [5,7],   empty: [1,2], shuffle: [30, 60] },
    { label: '困难', minSteps: 50, maxSteps: 80, colors: [7,9],   empty: [1,2], shuffle: [60, 100] },
    { label: '极难', minSteps: 80, maxSteps: 150,colors: [8,10],  empty: [1,1], shuffle: [100, 200] }
];
```

`shuffle` 表示反向生成时执行的随机移动次数范围。

### 5.3 玩家能力追踪

```javascript
playerSkill = {
    currentBracket: 2,      // 当前难度档位 (0~4)
    streak: 0,              // 连胜次数
    lastResults: []         // 最近10局结果 {win, time, usedHint, skipped}
};
```

**升降级规则**：
- 连胜 2 局 + 均未使用提示 → 升1档
- 连胜 3 局 → 升1档（即使用了提示）
- 使用跳过 或 一局内使用提示 > 2 次 → 降1档
- 连续 2 局用时超过 5 分钟 → 降1档

---

## 6. 待办清单

- [x] 基础HTML结构和样式
- [x] 基础游戏逻辑（倒水、撤销、胜负判断）
- [ ] 重写 generator.js：反向生成 + 难度参数
- [ ] 重写 solver.js：BFS最短解 + 难度评分
- [ ] 修复 layout.js：移除硬编码8列，优化自适应
- [ ] 增强 ui.js：倒水动画、错误摇晃、胜利特效
- [ ] 增加 core.js：自适应难度、玩家数据持久化
- [ ] 增加拖拽操作支持
- [ ] 音效开关（可选）
